setup:
  config:
    env_file: .env
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn -k uvicorn.workers.UvicornWorker --worker-tmp-dir /dev/shm config.wsgi -b 0.0.0.0:${PORT:-5000}
